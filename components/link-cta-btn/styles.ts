import { css } from "@emotion/react";

export const linkCtaBtnCss = css`
  background-color: var(--color-text-default);
  text-decoration: none;
  border-radius: var(--bor-rad-small);
  color: var(--color-bg);
  font-weight: 600;
  display: flex;
  outline: none;
  padding: var(--padding-small) var(--padding-mid);
`;
